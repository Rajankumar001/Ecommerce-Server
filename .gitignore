# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp/
.pnp.js

# Build/Output directories
dist/
build/
out/
.next/                   # Next.js build output
.nuxt/                   # Nuxt.js build output
.parcel-cache/           # Parcel build cache
.webpack/                # Webpack build artifacts (if not 'dist' or 'build')
es/                      # Common JS module output
lib/                     # Common JS module output (alternative)

# Dependency lock files (uncomment if you don't want to commit them, though generally recommended to commit)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
logs/

# Testing
coverage/                # Jest/Istanbul test coverage reports
.nyc_output/             # nyc (Istanbul) intermediate output

# Editor/IDE specific files
.vscode/                 # VS Code settings (if project-specific and personal)
.idea/                   # IntelliJ IDEA settings
.project                 # Eclipse settings
.classpath               # Eclipse settings
.settings/               # Eclipse settings
*.iml                    # IntelliJ IDEA module files
*.swp                    # Vim swap files
*.swo                    # Vim swap files
*~                       # Backup files created by some editors
.DS_Store                # macOS specific files
thumbs.db                # Windows thumbnail cache
Desktop.ini              # Windows system files
ehthumbs.db              # Windows thumbnail cache
.eslintcache             # ESLint cache
.prettierignore          # Usually committed, but sometimes ignored if user-specific
.editorconfig            # Usually committed, but sometimes ignored if user-specific